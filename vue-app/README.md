# 甜饼工坊 - Vue 3 + TypeScript 版本

这是「甜饼工坊」交易日记应用的 Vue 3 + TypeScript 重构版本。

## 项目结构

```
vue-app/
├── public/
│   ├── companies_tse.json    # 公司数据
│   └── icons/                # PWA 图标
├── src/
│   ├── assets/
│   │   └── styles.css        # 全局样式
│   ├── components/
│   │   ├── MainPage.vue      # 主页面
│   │   ├── AnalysisPage.vue  # 数据分析页
│   │   ├── DividendPage.vue  # 股东分红页
│   │   ├── DaySheet.vue      # 添加/编辑记录弹窗
│   │   ├── SettingsSheet.vue # 设置弹窗
│   │   └── ImportPasteSheet.vue # 粘贴导入弹窗
│   ├── composables/
│   │   └── useChart.ts       # 图表 composable
│   ├── services/
│   │   ├── database.ts       # IndexedDB 服务
│   │   └── company.ts        # 公司数据服务
│   ├── stores/
│   │   └── app.ts            # Pinia 状态管理
│   ├── types/
│   │   └── index.ts          # TypeScript 类型定义
│   ├── utils/
│   │   └── format.ts         # 格式化工具函数
│   ├── App.vue               # 根组件
│   └── main.ts               # 入口文件
├── index.html
├── package.json
├── tsconfig.json
└── vite.config.ts
```

## 安装与运行

```bash
# 如果 npm 缓存有权限问题，先运行：
sudo chown -R $(whoami) ~/.npm

# 安装依赖
npm install

# 开发模式
npm run dev

# 构建生产版本
npm run build

# 预览生产版本
npm run preview
```

## 技术栈

- **Vue 3** - 渐进式 JavaScript 框架
- **TypeScript** - 类型安全
- **Pinia** - Vue 状态管理
- **Chart.js** - 图表库
- **Vite** - 构建工具
- **vite-plugin-pwa** - PWA 支持

## 功能特性

- 📝 每日交易记录
- 📊 收益趋势图表
- 📈 数据分析（股票排行、月度收益等）
- 🎁 股东分红计算
- 💾 数据导入导出
- 📱 PWA 支持，可添加到主屏幕
- 🌙 自动深色/浅色主题

## 数据存储

所有数据使用 IndexedDB 存储在本地设备，不会上传到服务器。

